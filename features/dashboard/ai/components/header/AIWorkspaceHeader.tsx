export function AIWorkspaceHeader() {
  return (
    <section className="flex flex-col gap-2">
      <h1 className="text-3xl font-bold tracking-tight">
        AI Workspace
      </h1>

      <p className="text-muted-foreground">
        Manage AI providers, models, workspace defaults, inference settings,
        and model evaluation from a single provider-agnostic workspace.
      </p>
    </section>
  )
}
